.json {
  &__value {
    align-self: flex-end;
    align-items: flex-end;
    min-height 40px;
    flex: 1;
    text-align center;
    justify-content: center;
    flex-direction column;
    margin-top 10px;
    font-size 35px;
    line-height 30px;
    display block;
    width 100%;
    margin-bottom 9px;
  }
}

.icon {
  text-align center;
  font-size 100px;
  line-height 30px;
  width 100%;
}